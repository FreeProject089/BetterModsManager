@echo off
NET SESSION >nul 2>&1
if %errorLevel% == 0 (
    goto :run
) else (
    powershell -Command "Start-Process '%~f0' -Verb RunAs"
    exit /b
)
:run
cd /d "%~dp0"
title BMM Mini-Server
powershell -ExecutionPolicy Bypass -File ".\bmm-mini-server.ps1"
